---
name: commit
description: Stage all changes and create a git commit with a message summarizing the changes, including filenames.
---

Run `git status` and `git diff` (both staged and unstaged) to understand what has changed. Then stage all unstaged changes with `git add` and write a concise commit message that summarizes the changes and explicitly names the files involved. Commit using that message.
